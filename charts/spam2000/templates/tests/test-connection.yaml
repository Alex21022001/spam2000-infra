apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spam2000.fullname" . }}-test-connection"
  labels:
    {{- include "spam2000.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['-O', '-', '{{ include "spam2000.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
